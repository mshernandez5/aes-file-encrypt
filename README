To compile and link the program, run "make" in the source directory.
An executable named "AES128Encrypt" will be created.
To remove the generated executable and compiled object files, run "make clean".
